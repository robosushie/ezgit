const default_ezgit_config = `module.exports = {
  "exclude-file-list": ["package.json", "package-lock.json"],
  commands: {
    // you can add custom commands here
  },
}`;

module.exports = { default_ezgit_config };
